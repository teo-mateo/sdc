@model SDC.web.Models.Books.Book

@{
    ViewBag.Title = Model.Title;
}

<a class="btn btn-default margin-bottom" href="@Url.Action("Details", new { id = Model.Shelf.Id })">Go back</a>

<div class="box">
    <div class="box-header">
        <div class="box-title">@Model.Title</div>
    </div>
    <div class="box-body">

    </div>
</div>